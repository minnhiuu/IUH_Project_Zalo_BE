apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server-deploy
  namespace: bondhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-server
  template:
    metadata:
      labels:
        app: config-server
    spec:
      nodeSelector:
        node-type: infra
      containers:
        - name: config-server
          image: 952647925716.dkr.ecr.ap-southeast-2.amazonaws.com/bondhub/config-server:latest
          ports:
            - containerPort: 8888
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8888
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          envFrom:
            - configMapRef:
                name: bondhub-common-config
            - secretRef:
                name: bondhub-common-secrets
