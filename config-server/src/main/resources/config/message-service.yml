server:
  port: 8087

spring:
  application:
    name: message-service

  messages:
    basename: i18n/messages, i18n/messages-message

  # MongoDB Configuration
  data:
    mongodb:
      uri: ${MONGODB_URI_MESSAGES}
      auto-index-creation: true
      uuid-representation: standard

  # Kafka Configuration for Message Service
  kafka:
    # Producer configuration for outgoing message events
    producer:
      client-id: message-service-producer
      retries: 3
      properties:
        max.in.flight.requests.per.connection: 5
        spring.json.value.default.type: "com.bondhub.common.event.ai.AiMessageSaveEvent"
        spring.json.trusted.packages: "com.bondhub.common.event"
    # Consumer configuration for incoming events
    consumer:
      group-id: message-service-group
      client-id: message-service-consumer
      max-poll-records: 100

    # Listener configuration
    listener:
      concurrency: 3
      ack-mode: manual_immediate

# Service-specific logging overrides
logging:
  level:
    com.bondhub.messageservice: DEBUG
  file:
    name: logs/message-service.log

# Service-specific resilience4j overrides
resilience4j:
  circuitbreaker:
    instances:
      messageService:
        base-config: default

  retry:
    instances:
      messageService:
        base-config: default

  timelimiter:
    instances:
      messageService:
        base-config: default

# Service-specific security configuration
bondhub:
  security:
    public-endpoints: []

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  show-actuator: false

# AWS S3 Configuration
cloud:
  aws:
    region:
      static: ${AWS_REGION}
aws:
  s3:
    bucket:
      name: ${AWS_S3_BUCKET_NAME}
