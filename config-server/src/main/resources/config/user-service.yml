server:
  port: 8081

spring:
  application:
    name: user-service

  messages:
    basename: i18n/messages, i18n/messages-user

  data:
    mongodb:
      uri: ${MONGODB_URI_USERS}
      auto-index-creation: true
      uuid-representation: standard

  # Kafka Configuration for User Service
  kafka:
    # Producer configuration for user events
    producer:
      client-id: user-service-producer
      retries: 3
      properties:
        max.in.flight.requests.per.connection: 5

    # Consumer configuration for incoming events (if needed)
    consumer:
      group-id: user-service-group
      client-id: user-service-consumer
      max-poll-records: 100

    # Listener configuration
    listener:
      concurrency: 2
      ack-mode: manual

# Eureka Client Configuration
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}
    register-with-eureka: true
    fetch-registry: true
    registry-fetch-interval-seconds: 30
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90
    metadata-map:
      version: 0.0.1-SNAPSHOT
      environment: ${spring.profiles.active:default}

# Management and Monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,refresh,env
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      probes:
        enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

# Logging Configuration
logging:
  level:
    root: INFO
    com.bondhub.userservice: DEBUG
    org.springframework.web: INFO
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  file:
    name: logs/user-service.log
    max-size: 10MB
    max-history: 30
    total-size-cap: 1GB

# Resilience4j Circuit Breaker (if needed)
resilience4j:
  circuitbreaker:
    instances:
      userService:
        register-health-indicator: true
        sliding-window-size: 10
        minimum-number-of-calls: 5
        permitted-number-of-calls-in-half-open-state: 3
        wait-duration-in-open-state: 30s
        failure-rate-threshold: 50
        slow-call-rate-threshold: 100
        slow-call-duration-threshold: 3s

  retry:
    instances:
      userService:
        max-attempts: 3
        wait-duration: 1s

  timelimiter:
    instances:
      userService:
        timeout-duration: 5s

# BondHub Security Configuration
bondhub:
  security:
    gateway-auth:
      enabled: true
    public-endpoints:
      - /users
